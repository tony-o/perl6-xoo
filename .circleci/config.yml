defaults:
  steps:
    - checkout
    - run: tagged_release
#    - run: bin/ci build_perl6
#    - run: bin/ci publish_docker
version: 2
jobs:
  build:
    docker:
      - image: tonyodell/rakudo-nightly:test2
    
    working_directory: ~

    steps:
      - checkout
      - run: 
          name: install build deps
          command: |
              apt install -y libsqlite3-dev git
              zef install --deps-only .
      - run:
          name: test 
          command: |
              zef test .
